# 日志上传问题排查指南

## 问题描述

错误信息：`open upload\Task_63\test_1.log: the system cannot find the path specified`

## 可能原因分析

### 1. 文件路径问题
- **相对路径问题**：系统可能将 `upload\Task_63\test_1.log` 当作相对路径处理
- **路径分隔符问题**：Windows系统使用反斜杠 `\`，可能导致路径解析错误
- **工作目录问题**：当前工作目录与预期不符

### 2. 文件不存在
- 日志文件尚未创建
- 文件创建失败
- 文件被意外删除

### 3. 权限问题
- 文件访问权限不足
- 目录访问权限不足

## 排查步骤

### 步骤1：检查日志文件路径构建

查看 `TestCaseExecutionServiceImpl.handleProcessResult` 方法中的路径构建逻辑：

```java
String rootDir = fileStorageConfig != null ? fileStorageConfig.getRootDirectory() : System.getProperty("java.io.tmpdir");
Path rootDirectory = java.nio.file.Paths.get(rootDir);
Path taskDir = rootDirectory.resolve(request.getTaskId());
Path logsDir = taskDir.resolve("logs");
Path logFilePath = logsDir.resolve(logFileName);
```

**检查点：**
1. `fileStorageConfig.getRootDirectory()` 是否正确配置
2. `request.getTaskId()` 是否为 `Task_63`
3. `logFileName` 是否为 `test_1.log`

### 步骤2：验证文件是否存在

在日志上传前添加诊断代码：

```java
// 诊断日志文件路径问题
LogUploadDiagnosticTool.diagnoseLogFilePath(logFilePath.toString(), request.getTaskId(), 
        testCase.getTestCaseId(), testCase.getRound());
```

**检查点：**
1. 文件是否存在于预期路径
2. 文件大小是否大于0
3. 文件是否可读

### 步骤3：检查目录结构

确保以下目录结构存在：
```
{rootDirectory}/
  └── Task_63/
      └── logs/
          └── test_1.log
```

### 步骤4：检查gohttpserver配置

验证 `goHttpServerUrl` 配置：
- URL格式是否正确（应包含 `http://` 或 `https://`）
- 服务器是否可访问
- 上传接口是否正常

## 修复方案

### 方案1：增强路径验证

已在 `GoHttpServerClient.uploadLocalFile` 方法中添加：

```java
// 转换为绝对路径
Path sourcePath = Paths.get(localFilePath).toAbsolutePath();
log.info("解析后的绝对路径: {}", sourcePath.toString());

if (!Files.exists(sourcePath)) {
    throw new IOException("源文件不存在: " + sourcePath.toString() + " (原始路径: " + localFilePath + ")");
}
```

### 方案2：增强错误处理

已在 `TestCaseExecutionServiceImpl.handleProcessResult` 方法中添加：

```java
// 验证日志文件是否存在
if (!Files.exists(logFilePath)) {
    log.warn("日志文件不存在，跳过上传 - 用例ID: {}, 轮次: {}, 文件路径: {}", 
            testCase.getTestCaseId(), testCase.getRound(), logFilePath.toString());
} else {
    // 验证文件大小
    long fileSize = Files.size(logFilePath);
    if (fileSize == 0) {
        log.warn("日志文件为空，跳过上传 - 用例ID: {}, 轮次: {}, 文件路径: {}", 
                testCase.getTestCaseId(), testCase.getRound(), logFilePath.toString());
    } else {
        // 执行上传
    }
}
```

### 方案3：添加诊断工具

创建了 `LogUploadDiagnosticTool` 类，提供详细的路径诊断信息。

## 常见问题解决

### 问题1：路径分隔符问题

**现象**：Windows系统上路径使用反斜杠导致问题

**解决**：使用 `Paths.get()` 和 `Path.resolve()` 方法，自动处理路径分隔符

### 问题2：相对路径问题

**现象**：系统将路径当作相对路径处理

**解决**：使用 `Path.toAbsolutePath()` 转换为绝对路径

### 问题3：文件创建时机问题

**现象**：上传时文件尚未创建

**解决**：在 `PythonExecutorUtil.startOutputReader` 中确保文件创建完成

### 问题4：权限问题

**现象**：文件存在但无法读取

**解决**：检查文件权限，确保应用有读取权限

## 调试建议

### 1. 启用详细日志

在 `application.yml` 中设置：

```yaml
logging:
  level:
    com.caseexecute.util.GoHttpServerClient: DEBUG
    com.caseexecute.service.impl.TestCaseExecutionServiceImpl: DEBUG
```

### 2. 检查系统属性

添加日志输出：

```java
log.info("当前工作目录: {}", System.getProperty("user.dir"));
log.info("系统临时目录: {}", System.getProperty("java.io.tmpdir"));
log.info("文件存储根目录: {}", fileStorageConfig.getRootDirectory());
```

### 3. 验证文件系统

在文件操作前验证：

```java
Path testPath = Paths.get("test.txt");
log.info("测试路径解析: {}", testPath.toAbsolutePath().toString());
```

## 预防措施

### 1. 路径标准化

始终使用 `Path` 对象处理文件路径，避免字符串拼接

### 2. 异常处理

为所有文件操作添加适当的异常处理

### 3. 日志记录

记录关键路径信息，便于问题排查

### 4. 单元测试

为文件路径处理逻辑编写单元测试

## 联系支持

如果问题仍然存在，请提供：

1. 完整的错误日志
2. 系统环境信息（操作系统、Java版本）
3. 配置文件内容
4. 诊断工具输出结果
