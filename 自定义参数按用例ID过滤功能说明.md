# 自定义参数按用例ID过滤功能说明

## 功能概述

执行机在解析自定义参数时，现在支持根据用例ID获取属于自己的参数，实现参数的精确定位和隔离。

## 支持的参数格式

### 1. 对象格式（按用例ID分组）

```json
{
  "123": [
    {"key": "timeout", "value": "30"},
    {"key": "retry_count", "value": "3"}
  ],
  "456": [
    {"key": "timeout", "value": "60"},
    {"key": "max_connections", "value": "10"}
  ]
}
```

### 2. 数组格式（包含用例ID字段）

```json
[
  {
    "testCaseId": 123,
    "key": "timeout",
    "value": "30"
  },
  {
    "testCaseId": 123,
    "key": "retry_count",
    "value": "3"
  },
  {
    "testCaseId": 456,
    "key": "timeout",
    "value": "60"
  }
]
```

## 功能特性

### 1. 精确过滤
- 执行机只会获取与当前用例ID匹配的参数
- 避免不同用例之间的参数干扰
- 提高参数管理的精确性

### 2. 向后兼容
- 保持对原有参数格式的兼容性
- 如果不指定用例ID，会解析所有参数（原有行为）
- 支持多种参数格式的混合使用

### 3. 灵活配置
- 支持不同用例使用不同的参数值
- 支持用例级别的参数定制
- 便于参数的管理和维护

## 使用示例

### 场景1：不同用例使用不同超时时间

```json
{
  "1001": [
    {"key": "timeout", "value": "30"}
  ],
  "1002": [
    {"key": "timeout", "value": "60"}
  ],
  "1003": [
    {"key": "timeout", "value": "120"}
  ]
}
```

### 场景2：用例特定的配置参数

```json
[
  {
    "testCaseId": 1001,
    "key": "network_type",
    "value": "4G"
  },
  {
    "testCaseId": 1001,
    "key": "data_size",
    "value": "10MB"
  },
  {
    "testCaseId": 1002,
    "key": "network_type",
    "value": "5G"
  },
  {
    "testCaseId": 1002,
    "key": "data_size",
    "value": "50MB"
  }
]
```

## 日志输出示例

### 成功解析日志
```
开始处理自定义参数: {"123":[{"key":"timeout","value":"30"}]}, 用例ID: 123
解析对象格式自定义参数: {"123":[{"key":"timeout","value":"30"}]}
解析到用例123的参数: key=timeout, value=30
成功解析用例123的自定义参数: 解析出1个参数
解析用例123的自定义参数结果: {timeout=30}, 参数数量: 1
添加采集任务自定义参数: --timeout "30"
```

### 未找到参数日志
```
开始处理自定义参数: {"123":[{"key":"timeout","value":"30"}]}, 用例ID: 456
解析对象格式自定义参数: {"123":[{"key":"timeout","value":"30"}]}
未找到用例456的自定义参数配置
解析用例456的自定义参数结果: {}, 参数数量: 0
```

## 技术实现

### 1. 方法重载
- `parseCustomParams(String customParams)` - 原有方法，解析所有参数
- `parseCustomParams(String customParams, Long testCaseId)` - 新方法，按用例ID过滤

### 2. 过滤逻辑
- 对象格式：根据用例ID作为key查找对应的参数数组
- 数组格式：遍历数组，匹配testCaseId字段
- 未匹配：返回空参数Map，不影响执行

### 3. 错误处理
- JSON解析失败：记录警告日志，返回空参数Map
- 格式不支持：记录警告日志，返回空参数Map
- 参数验证：确保key和value都不为空

## 注意事项

1. **用例ID匹配**：确保参数中的用例ID与实际的用例ID完全匹配
2. **参数格式**：严格按照JSON格式编写参数，避免语法错误
3. **向后兼容**：现有系统可以继续使用原有参数格式
4. **性能考虑**：参数解析在用例执行时进行，建议控制参数数量

## 升级建议

1. **逐步迁移**：可以逐步将现有参数迁移到新的格式
2. **测试验证**：在生产环境使用前，充分测试参数解析功能
3. **监控日志**：关注参数解析的日志输出，确保正确匹配
4. **文档更新**：更新相关文档，说明新的参数格式要求


